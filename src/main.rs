//use fsm_trait_wasm::{FSM, SimpleFiniteStateMachine};

fn main() {

    /*let mut test = SimpleFiniteStateMachine::new("Test");
    let name = 0;
    let name2 = 1;
    let name3 = 2;

    test.add_node(name);
    test.add_node(name2);
    test.add_node(name3);

    test.add_transition('a',0,1);
    test.add_transition('b',0,2);
    test.add_transition('c',1,2);
    test.add_transition('d',2,2);

    test.display_fsm();

    let word1 = "acd";
    let mut result = test.process_fsm(0,word1);
    let lenght = result.len();
    print!("Taille du chemin pour ce mot.");
    print!("{}",lenght);
    dbg!(result);*/
}